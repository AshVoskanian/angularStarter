export enum ErrorMessageType {
  Error = "error",
  Warning = "warning",
  Success = "success",
}

export enum IconType {
  Info = "info",
  Check = "check",
  Clock = "clock",
  Phone = "phone",
  Mail = "mail",
}
